<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingBottom="24dp"
    android:background="?attr/colorSurfaceContainer"
    android:fitsSystemWindows="true"
    tools:context=".ui.home.DashboardFragment">

    <!-- ELIMINADO: El TextView con el título fijo "Gestión Reparaciones"
         para evitar duplicidad. -->

    <!-- Título de Bienvenida (MVVM se encargará de rellenarlo) -->
    <TextView
        android:id="@+id/tvBienvenida"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="32dp"
        android:layout_marginBottom="40dp"
        android:textAppearance="?attr/textAppearanceHeadlineSmall"
        android:textColor="?attr/colorPrimary"
        tools:text="¡Bienvenido, Esteban Martínez!" />

    <!-- Botones de Acción Principales (Material 3) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center_horizontal">

        <!-- Botón 1: Ventas -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnVentas"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/ventas"
            android:layout_marginBottom="16dp"
            android:paddingVertical="12dp"
            android:textAppearance="?attr/textAppearanceTitleMedium"
            style="@style/Widget.Material3.Button"/>

        <!-- Botón 2: Reparaciones -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnReparaciones"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/reparaciones"
            android:layout_marginBottom="16dp"
            android:paddingVertical="12dp"
            android:textAppearance="?attr/textAppearanceTitleMedium"
            style="@style/Widget.Material3.Button"/>

        <!-- Botón 3: Clientes -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnClientes"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/clientes"
            android:layout_marginBottom="16dp"
            android:paddingVertical="12dp"
            android:textAppearance="?attr/textAppearanceTitleMedium"
            style="@style/Widget.Material3.Button"/>

        <!-- Botón 4: Productos -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnProductos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/productos"
            android:paddingVertical="12dp"
            android:textAppearance="?attr/textAppearanceTitleMedium"
            style="@style/Widget.Material3.Button"/>

    </LinearLayout>
</LinearLayout>